# transmission-psscripts

`Remove-TransmissionPublicTorrents.ps1` removes completed public torrents by default.

Use `-RemovePrivateOlderThan30Days` to also remove completed private torrents whose `dateAdded` is older than 30 days.
